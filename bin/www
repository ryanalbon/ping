#!/usr/bin/env node

const express = require('express');
const http = require('http');

const app = express();

app.get('/ping', (req, res) => res.status(200).json({ message: 'Pong!' }).end());

http.createServer(app).listen('8080', '0.0.0.0');
